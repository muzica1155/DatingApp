export const environment = {
  production: true,
  // hostURl: 'https://prod.site',
  // username: 'user1',
  // token: 'abcxyzProd',
  // port:8080,
  // envName: 'prod', 
  apiUrl: 'api/',//when we publish our application to we r going to publish our annual application & host it from our ast dotnet core app 
  hubUrl: 'hubs/'
  //cahnges during signalr same as evironment we need to do same bcoz when we publish we dont run into any problems
  //hubUrl: 'hubs/',// after this create a service to track the online presence inside our annual client 

};
